// URL de base de l'API backend Express.
// Si l'adresse ou le port du backend changent un jour, c'est ici
// et uniquement ici qu'il faut le mettre à jour.
export const API_URL = 'http://localhost:4000/api'
